# Setup Selfsign certificate for localhost


using dotnet dev-certs command

```bash
dotnet dev-certs https -ep localhost.crt -np --trust --format pem
```

dotnet dev-certs https -ep localhost.crt -np --trust --format pem
